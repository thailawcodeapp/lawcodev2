diff --git a/node_modules/@capgo/native-audio/android/gradle.properties b/node_modules/@capgo/native-audio/android/gradle.properties
new file mode 100644
index 0000000..489bf36
--- /dev/null
+++ b/node_modules/@capgo/native-audio/android/gradle.properties
@@ -0,0 +1,5 @@
+
+
+# NativeAudio Optional Dependencies (auto-generated)
+# Generated by NativeAudio hook script
+nativeAudio.hls.include=true
diff --git a/node_modules/@capgo/native-audio/android/src/main/java/ee/forgr/audio/NativeAudio.java b/node_modules/@capgo/native-audio/android/src/main/java/ee/forgr/audio/NativeAudio.java
index 6385931..568697e 100644
--- a/node_modules/@capgo/native-audio/android/src/main/java/ee/forgr/audio/NativeAudio.java
+++ b/node_modules/@capgo/native-audio/android/src/main/java/ee/forgr/audio/NativeAudio.java
@@ -1032,7 +1032,19 @@ public class NativeAudio extends Plugin implements AudioManager.OnAudioFocusChan
         updateTrackedPlaybackState(assetId, PlaybackStateCompat.STATE_STOPPED);
 
         if (assetId != null && assetId.equals(currentlyPlayingAssetId)) {
-            clearNotification();
+            // PATCH: the notification is deliberately NOT cleared here.
+            // An app that plays a queue of clips loads one asset per clip, so
+            // every boundary between two clips passed through this method and
+            // took the media notification away for as long as it took the app
+            // to start the next one. The result was a lock-screen player that
+            // vanished and came back on every paragraph. Ownership is still
+            // released (below), so the next play() re-posts the notification
+            // with its own metadata; nothing else relied on the clear.
+            //
+            // A single clip that plays to its end and is never followed does
+            // leave the notification up until the asset is unloaded — which is
+            // the app's own signal that playback is over, and a better default
+            // than blanking the card between tracks.
             currentlyPlayingAssetId = null;
         }
         notifyPlaybackState(assetId, "complete");
@@ -1558,6 +1570,8 @@ public class NativeAudio extends Plugin implements AudioManager.OnAudioFocusChan
                 PlaybackStateCompat.ACTION_STOP |
                 PlaybackStateCompat.ACTION_REWIND |
                 PlaybackStateCompat.ACTION_FAST_FORWARD |
+                PlaybackStateCompat.ACTION_SKIP_TO_NEXT |
+                PlaybackStateCompat.ACTION_SKIP_TO_PREVIOUS |
                 PlaybackStateCompat.ACTION_SEEK_TO
         );
         mediaSession.setPlaybackState(stateBuilder.build());
@@ -1632,6 +1646,26 @@ public class NativeAudio extends Plugin implements AudioManager.OnAudioFocusChan
                     });
                 }
 
+                // Upstream registers no skip-to-next/previous at all, so the
+                // notification had no way to offer them. The playlist lives in
+                // JS: these report the press and change nothing here, because
+                // only the app knows what the next section is.
+                @Override
+                public void onSkipToNext() {
+                    handleCurrentMediaAction("skipping to next", (audioId, asset) -> {
+                        notifyPlaybackState(audioId, "remoteNextTrack");
+                        Log.d(TAG, "Skip to next track");
+                    });
+                }
+
+                @Override
+                public void onSkipToPrevious() {
+                    handleCurrentMediaAction("skipping to previous", (audioId, asset) -> {
+                        notifyPlaybackState(audioId, "remotePreviousTrack");
+                        Log.d(TAG, "Skip to previous track");
+                    });
+                }
+
                 @Override
                 public void onSeekTo(long pos) {
                     handleCurrentMediaAction("seeking", (audioId, asset) -> {
@@ -1719,11 +1753,11 @@ public class NativeAudio extends Plugin implements AudioManager.OnAudioFocusChan
             // Add actions BEFORE setStyle() for proper wiring
             .addAction(
                 new NotificationCompat.Action.Builder(
-                    android.R.drawable.ic_media_rew,
-                    "Rewind 15 seconds",
+                    android.R.drawable.ic_media_previous,
+                    "Previous",
                     androidx.media.session.MediaButtonReceiver.buildMediaButtonPendingIntent(
                         getContext(),
-                        PlaybackStateCompat.ACTION_REWIND
+                        PlaybackStateCompat.ACTION_SKIP_TO_PREVIOUS
                     )
                 ).build()
             )
@@ -1739,11 +1773,11 @@ public class NativeAudio extends Plugin implements AudioManager.OnAudioFocusChan
             )
             .addAction(
                 new NotificationCompat.Action.Builder(
-                    android.R.drawable.ic_media_ff,
-                    "Fast forward 15 seconds",
+                    android.R.drawable.ic_media_next,
+                    "Next",
                     androidx.media.session.MediaButtonReceiver.buildMediaButtonPendingIntent(
                         getContext(),
-                        PlaybackStateCompat.ACTION_FAST_FORWARD
+                        PlaybackStateCompat.ACTION_SKIP_TO_NEXT
                     )
                 ).build()
             )
@@ -1793,6 +1827,8 @@ public class NativeAudio extends Plugin implements AudioManager.OnAudioFocusChan
                     PlaybackStateCompat.ACTION_STOP |
                     PlaybackStateCompat.ACTION_REWIND |
                     PlaybackStateCompat.ACTION_FAST_FORWARD |
+                    PlaybackStateCompat.ACTION_SKIP_TO_NEXT |
+                    PlaybackStateCompat.ACTION_SKIP_TO_PREVIOUS |
                     PlaybackStateCompat.ACTION_SEEK_TO
             );
         mediaSession.setPlaybackState(stateBuilder.build());
diff --git a/node_modules/@capgo/native-audio/ios/Sources/NativeAudioPlugin/Plugin.swift b/node_modules/@capgo/native-audio/ios/Sources/NativeAudioPlugin/Plugin.swift
index 0c4d7d3..e58d590 100644
--- a/node_modules/@capgo/native-audio/ios/Sources/NativeAudioPlugin/Plugin.swift
+++ b/node_modules/@capgo/native-audio/ios/Sources/NativeAudioPlugin/Plugin.swift
@@ -221,8 +221,15 @@ public class NativeAudio: CAPPlugin, AVAudioPlayerDelegate, CAPBridgedPlugin {
 
     internal func handlePlaybackCompletion(assetId: String, audioAsset: AudioAsset? = nil) {
         if currentlyPlayingAssetId == assetId {
+            // PATCH: Now Playing is deliberately NOT cleared here.
+            // An app playing a queue loads one asset per clip, so every
+            // boundary between two clips ran through here and blanked the
+            // lock-screen card until the next clip started — the player
+            // disappeared and came back on every paragraph. Ownership is still
+            // released, so the next play() writes its own Now Playing info.
+            // The card is cleared where it belongs: unload(), which the app
+            // calls when the queue is actually over.
             currentlyPlayingAssetId = nil
-            clearNowPlayingInfo()
         }
         notifyPlaybackState(assetId: assetId, reason: "complete", state: .stopped, audioAsset: audioAsset)
     }
@@ -373,9 +380,32 @@ public class NativeAudio: CAPPlugin, AVAudioPlayerDelegate, CAPBridgedPlugin {
             return .success
         }
 
-        // Skip forward command
+        // Next / previous track. Upstream registers neither, so no lock screen
+        // ever shows skip buttons and iOS's skip-forward seeks inside the clip
+        // instead — pressing it on a short paragraph reads as "restart this
+        // section". The playlist lives in JS, so these report the press and
+        // nothing else; the app decides what "next" means.
+        commandCenter.nextTrackCommand.isEnabled = true
+        commandCenter.nextTrackCommand.addTarget { [weak self] _ in
+            guard let self else { return .commandFailed }
+            guard let assetId = self.currentlyPlayingAssetId else { return .commandFailed }
+            self.notifyPlaybackState(assetId: assetId, reason: "remoteNextTrack")
+            return .success
+        }
+
+        commandCenter.previousTrackCommand.isEnabled = true
+        commandCenter.previousTrackCommand.addTarget { [weak self] _ in
+            guard let self else { return .commandFailed }
+            guard let assetId = self.currentlyPlayingAssetId else { return .commandFailed }
+            self.notifyPlaybackState(assetId: assetId, reason: "remotePreviousTrack")
+            return .success
+        }
+
+        // Skip forward command. Disabled: iOS gives a lock screen two side
+        // buttons, and it picks skip-interval over next-track when both are
+        // registered. This app wants sections, not fifteen seconds.
         commandCenter.skipForwardCommand.preferredIntervals = [NSNumber(value: 15)]
-        commandCenter.skipForwardCommand.isEnabled = true
+        commandCenter.skipForwardCommand.isEnabled = false
         commandCenter.skipForwardCommand.addTarget { [weak self] event in
             guard let self else { return .commandFailed }
             guard let skipEvent = event as? MPSkipIntervalCommandEvent else { return .commandFailed }
@@ -384,7 +414,7 @@ public class NativeAudio: CAPPlugin, AVAudioPlayerDelegate, CAPBridgedPlugin {
 
         // Skip backward command
         commandCenter.skipBackwardCommand.preferredIntervals = [NSNumber(value: 15)]
-        commandCenter.skipBackwardCommand.isEnabled = true
+        commandCenter.skipBackwardCommand.isEnabled = false
         commandCenter.skipBackwardCommand.addTarget { [weak self] event in
             guard let self else { return .commandFailed }
             guard let skipEvent = event as? MPSkipIntervalCommandEvent else { return .commandFailed }
